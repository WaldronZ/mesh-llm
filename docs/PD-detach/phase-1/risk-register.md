# Risk Register

Generated: 2026-05-18

This register records documentation drift found while preparing takeover docs.
When docs and code disagree, code is treated as the source of truth.

Owner confirmation, 2026-05-18: `mesh-llm/1` is the current formal runtime
protocol; `mesh-llm/0` is retained historical/legacy compatibility code.
`v0.60.0` is the minimum supported node software version for peers that
advertise a parseable version.

| ID | Severity | Area | Code truth | Conflicting or risky docs | Risk | Suggested action |
|---|---|---|---|---|---|---|
| RISK-001 | Medium | Protocol docs | Shared protocol code still defines `ALPN_V0 = b"mesh-llm/0"` and `ControlProtocol::JsonV0`; current host runtime protocol path defines/connects `ALPN_V1` only and host endpoint setup advertises `ALPN_V1` plus Skippy stage ALPN. Owner confirmed `/0` is historical/legacy compatibility code. | Older compatibility notes may imply `/0` is still a current formal runtime protocol. | Maintainers may assume host runtime still formally serves `/0`, or remove legacy compatibility code without an explicit cleanup decision. | Update main protocol docs to state that `mesh-llm/1` is current formal runtime protocol and `/0` is retained legacy compatibility code. |
| RISK-002 | Medium | Branch mesh compatibility docs | `PD-detach` code in `crates/mesh-llm-host-runtime/src/mesh/gossip.rs` rejects peers with parseable versions below `v0.60.0` at direct/transitive ingest and outbound rebroadcast; unknown/unparseable versions are conservatively allowed. Owner confirmed `v0.60.0` is the minimum supported node software version. | General compatibility guidance says mixed versions coexist without naming the floor. | Operators may be surprised when nodes explicitly advertising versions below `v0.60.0` are filtered. | Document `v0.60.0` as the minimum supported node software version, include the unknown-version nuance, and validate mixed-version QA. |
| RISK-003 | High | Architecture paths | Current host code is under `crates/mesh-llm-host-runtime/src/`; `crates/mesh-llm/src/` is a wrapper. | `docs/design/DESIGN.md`, repo notes, and older docs cite monolithic `crates/mesh-llm/src/api`, `src/network`, `src/inference`. | New maintainers edit or audit the wrong paths. | Promote `ARCHITECTURE_CURRENT.md`; update stale path maps. |
| RISK-004 | Medium | Static site | Runtime now uses embedded Skippy/host runtime paths. | `docs/index.html` still shows old `llama-server`/`rpc-server` style labels. | Public docs site may mislead users about runtime architecture. | Refresh landing page or move site assets away from source docs. |
| RISK-005 | Medium | Skippy | Embedded split serving exists in `crates/skippy-*` and `crates/mesh-llm-host-runtime/src/inference/skippy/`. | `docs/SKIPPY.md` and `docs/design/LLAMA_STAGE_INTEGRATION_PLAN.md` read as active integration plans. | Maintainers may treat completed migration plans as current TODOs. | Archive or rewrite as historical design notes. |
| RISK-006 | Medium | Owner identity | Owner keystore, signing, verification, trust policy, and revocation exist in code. | `docs/design/NODE_OWNER_IDENTITY.md` and `IDENTITY_INCIDENT_RESPONSE.md` read as proposal/draft. | Security runbooks may not match actual CLI UX. | Convert to current runbook or split proposal history from shipped workflow. |
| RISK-007 | Medium | Router | Current routing lives in `crates/mesh-llm-host-runtime/src/network/router.rs` and `crates/mesh-llm-routing/`. | `docs/design/MODEL_ROUTER.md`, `ROUTER_V2.md`, and `ROUTER_BENCHMARKS.md` include plan/stale benchmark state. | Routing changes may be based on old plans. | Reconcile router docs with current metrics and routing code. |
| RISK-008 | Medium | Benchmarks | Current benchmark scripts include `scripts/family-certify.sh`, `scripts/skippy-openai-smoke.sh`, `scripts/run-llama-benchy-openai.sh`. | `docs/BENCHMARKS.md`, `docs/skippy/BENCHMARK_TODO.md`, and some Skippy docs contain older commands or dated claims. | Performance claims may be stale or unreproducible. | Mark benchmark docs as evidence logs unless rerun and dated. |
| RISK-009 | Medium | OpenAI smoke script naming | Current script found: `scripts/skippy-openai-smoke.sh`. | `docs/skippy/BENCHMARK_TODO.md` and `docs/skippy/LLAMA_BENCHY.md` mention `scripts/openai-smoke.sh`. | Operators may run a missing script. | Update command names or add a compatibility wrapper if intended. |
| RISK-010 | Medium | Context planning | Context planning code exists in `crates/mesh-llm-host-runtime/src/runtime/context_planning.rs` and GGUF parsing in `crates/model-artifact/`. | `docs/specs/context-and-slots-auto.md` cites older paths. | Config/runtime sizing changes may miss current implementation. | Refresh path refs and confirm shipped behavior. |
| RISK-011 | Medium | Plugin v2 | Plugin host, stapler, MCP, and built-ins exist in code. | `docs/plugins/PLAN.md` reads as active roadmap. | Plugin API contract may be inferred from stale plan state. | Convert plan to status or archive; make `docs/plugins/README.md` the spec. |
| RISK-012 | Low | GPU benchmark CLI | GPU benchmark command exists in CLI/system code. | `docs/plans/gpu-benchmark-cli.md` remains a plan. | Duplicated planning doc may confuse current status. | Archive as implementation history. |
| RISK-013 | Low | Qwen evidence | Qwen docs contain lab-specific, time-sensitive benchmark evidence. | `docs/skippy/family/qwen*.md` may look like current runbooks. | Users may treat dated lab data as current customer procedure. | Split stable runbook from evidence log. |
| RISK-014 | Low | Jan integration | Local API crates exist, but external Jan plugin status is not verified. | `docs/design/JAN_MESH_API_INTEGRATION.md` may read as active integration. | Takeover may assume external maintenance status from local docs. | Confirm owner/status before planning work. |
